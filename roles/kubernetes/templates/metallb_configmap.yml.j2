---
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: default
      protocol: layer2
      addresses:
      - {{ ansible_facts['eth0']['ipv4']['address'] }}-{{ ansible_facts['eth0']['ipv4']['address'] }}